# Environment / secrets — never commit the real .env
.env

# Go build artifacts
seerr-app
*.exe

# Go module cache (generated by Dockerfile build, not needed in repo)
go.sum

# Docker
*.log

# OS
.DS_Store
Thumbs.db
